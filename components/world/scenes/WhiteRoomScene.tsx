// Deliberately close to empty — design spec §18: "remove almost everything
// ... just the character and one statement." No walls, no props, no
// activation lighting, and (per docs/DESIGN_DIRECTION.md Rev. 2's "no
// character" decision) no character either — the visitor's own first-person
// camera is the only presence here. The emotional pause of the journey.
export default function WhiteRoomScene() {
  return null;
}
