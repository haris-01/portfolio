// Set `url` once a real character model has been added (see
// docs/3D_MODELS.md) — path is relative to /public, e.g.
// '/models/character.glb'. Leave it null to keep the primitive placeholder
// that CharacterModel falls back to; nothing else needs to change.
export const CHARACTER_MODEL = {
  url: null as string | null,
  scale: 1,
};
